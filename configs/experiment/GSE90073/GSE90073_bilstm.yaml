# @package _global_

# to execute this experiment run:
# python train.py experiment=GSE90073_bilstm
defaults:
  - override /data: GSE90073
  - override /model: bilstm
  - override /callbacks: default
  - override /trainer: default
  - override /logger: wandb

tags: ["GSE90073", "bilstm", "case_control"]

seed: 42

trainer:
  min_epochs: 10
  gradient_clip_val: 0.5

callbacks:
  early_stopping:
    monitor: "val/loss"
    mode: "min"
  
  model_checkpoint:
    save_top_k: 3

logger:
  wandb:
    name: "GSE90073-BiLSTM-w${model.net.window_size}-h${model.net.hidden_size}-L${model.net.num_layers}"
    tags: ${tags}
    group: "GSE90073_classification"