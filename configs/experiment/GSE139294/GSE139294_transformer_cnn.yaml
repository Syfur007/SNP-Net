# @package _global_

# to execute this experiment run:
# python train.py experiment=GSE139294_transformer_cnn

defaults:
  - override /data: GSE139294
  - override /model: transformer_cnn
  - override /callbacks: default
  - override /trainer: cpu
  - override /logger: wandb

tags: ["lung_adenocarcinoma", "transformer_cnn", "case_control"]

seed: 42

trainer:
  min_epochs: 10
  gradient_clip_val: 0.5

callbacks:
  early_stopping:
    monitor: "val/loss"
    mode: "min"
  
  model_checkpoint:
    save_top_k: 3

logger:
  wandb:
    name: "GSE139294-TransformerCNN-dm${model.net.d_model}-nhead${model.net.nhead}-cnn${model.net.cnn_channels}"
    tags: ${tags}
    group: "GSE139294_classification"
