# @package _global_

# to execute this experiment run:
# python train.py experiment=GSE139294_lstm

defaults:
  - override /data: GSE139294
  - override /model: lstm
  - override /callbacks: default
  - override /trainer: xpu
  - override /logger: wandb

# LSTM for GSE139294 classification

tags: ["GSE139294", "lstm", "sequence", "case_control"]

seed: 42

trainer:
  min_epochs: 10
  gradient_clip_val: 1.0
  
callbacks:
  early_stopping:
    monitor: "val/loss"
    mode: "min"
  
  model_checkpoint:
    save_top_k: 3

logger:
  wandb:
    name: "GSE139294-LSTM-w${model.net.window_size}-h${model.net.hidden_size}-L${model.net.num_layers}"
    tags: ${tags}
    group: "GSE139294_classification"
  aim:
    experiment: "GSE139294_lstm"