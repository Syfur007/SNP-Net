# @package _global_

# to execute this experiment run:
# python train.py experiment=GSE139294_bilstm
defaults:
  - override /data: GSE139294
  - override /model: bilstm
  - override /callbacks: default
  - override /trainer: xpu
  - override /logger: wandb

tags: ["lung_adenocarcinoma", "bilstm", "case_control"]

seed: 42

trainer:
  min_epochs: 10
  gradient_clip_val: 0.5

callbacks:
  early_stopping:
    monitor: "val/loss"
    mode: "min"
  
  model_checkpoint:
    save_top_k: 3

logger:
  wandb:
    name: "GSE139294-lung_adenocarcinoma-BiLSTM-h${model.net.hidden_size}-L${model.net.num_layers}"
    tags: ${tags}
    group: "GSE139294_classification"